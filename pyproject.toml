[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jarvis-agent"
version = "0.1.0"
description = "A safety-first local Windows AI automation agent"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.31,<3",
  "python-dotenv>=1.0,<2",
  "APScheduler>=3.10,<4",
]

[project.optional-dependencies]
providers = [
  "google-genai>=1.0,<2",
]
windows = [
  "pyautogui>=0.9,<1",
  "pywin32>=306,<310",
  "pywinauto>=0.6,<1",
  "mss>=9,<11",
  "psutil>=5.9,<8",
  "send2trash>=1.8,<2",
  "opencv-python>=4.8,<5",
]
multimodal = [
  "Pillow>=10,<13",
  "pypdf>=5,<7",
]
voice = [
  "faster-whisper>=1.0,<2",
  "pyttsx3>=2.90,<3",
  "sounddevice>=0.4,<1",
  "soundfile>=0.12,<1",
]
dev = [
  "pytest>=8,<9",
  "ruff>=0.6,<1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.setuptools]
py-modules = ["audit", "config", "dispatcher", "main", "presence", "safety", "scheduler", "ui", "ui_config", "visual_presence"]
packages = ["llm", "skills", "memory"]
